# {slug} Status
description:: design-phase completeness for {slug} — one line per facet, on the `none < MVP-agent < MVP-user < Full-agent < Full-user` ladder

prd::          {{TIER}} ({{DATE}}) — {{why this tier; required for *-user cells, recommended for *-agent}}
ux::           none
architecture:: none
testing::      none
roadmap::      none
