---
description: "the artifact exercised as a user would; slow, and the tier that catches wiring rather than logic"
---
:>> [[DAS]] → [[FEX]] → [[DVT]] → [[DVTT]] → [Devtools E2E](hook://p/Devtools%20E2E)
# Devtools E2E
The artifact exercised as a user would; slow, and the tier that catches wiring rather than logic.

Part of [[DVTT|Devtools Test]], which holds the gate this tool serves. A leaf in the two-level shape: it has a breadcrumb rather than a masthead, because it fronts nothing and organizes no one.
