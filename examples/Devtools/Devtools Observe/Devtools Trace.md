---
description: "per-request spans; how a latency budget breach is attributed to a hop"
---
:>> [[DAS]] → [[FEX]] → [[DVT]] → [[DVTO]] → [Devtools Trace](hook://p/Devtools%20Trace)
# Devtools Trace
Per-request spans; how a latency budget breach is attributed to a hop.

Part of [[DVTO|Devtools Observe]], which holds the gate this tool serves. A leaf in the two-level shape: it has a breadcrumb rather than a masthead, because it fronts nothing and organizes no one.
