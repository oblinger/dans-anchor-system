---
description: "re-scan when a watched root changes, without a restart."
---

# [[HBR]] · T005 — Watched-root hot reload
re-scan when a watched root changes, without a restart.

next:: Watch the configured roots with an fs-event watcher and re-run the Scanner on the changed subtree only, without restarting the daemon.

## Summary

re-scan when a watched root changes, without a restart.

## Status

**Ready** — minted from the backlog row by `state` on 2026-08-28 (F614: every task has its doc; the row is its pointer).
