---
description: "deliberately empty — the target every pretend link in FEX points at, so clicking one creates nothing"
---
:>> [[DAS]] → [[examples]] → [FEX Empty](hook://p/FEX%20Empty) 
# FEX Empty
The page a made-up link lands on. A FEX example that needs a link to a part it does not build out writes `[[FEX Empty\|Part name]]` — the link renders as the part, resolves here, and never mints a stray page.
