---
description: MUSE queries — mechanically rendered from the backlog (Blockers / Ready+Next / Questions / Blocked / User / Other), and copied verbatim into Q.md. Do not hand-edit; edit the backlog rows.
---

# [A]  [[MUSE|MUSE]]  -  Ready 8    User 0   |   Now 6    Next 2    Later 1   |   Parked 0    Waiting 1    Icebox 0

## Ready
- [[MUSE003 - MUSE ingest is dead under launchd every drop since 2026-07-21 was|T003]] — **Next:** reproduce the failure under `_trust` rather than under a login shell — run `/Users/oblinger/bin/_trust muse-sweep` directly and capture the real per-file error, which the current log swallows behind the "repeatedly-failing" skip....
- [[MUSE004 - The sweep cannot report its own death three silent-failure fixes|T004]] — **Next:** three fixes, in order of how loud they make a failure. **(1) The identity gap is the root cause** — the same file succeeds by hand and fails under `_trust muse-sweep`....
- [[MUSE006 - Captures prepend to the top of Quick.md, landing them inside today's|T006]] — **Next:** Find the line in the MUSE capture path that writes to `LST/Quick.md` and change the insertion point from "top of file" to "after the first blank line following the head." Note the file gained an H1...
- [[MUSE007 - e2e-test.sh picks its fixture by smallest file size, so it selects|T007]] — **Next:** Make the fixture choice explicit rather than emergent. Prefer a **pinned, committed** sample beside the script over any `find` over the user's corpus — it removes the dependence on what Dan happens to...
- [[MUSE009 - A successfully-ingested recording sat in.muse.failures, and the false|T009]] — **Next:** Make ingest success remove the file's row from `.muse.failures` — and, because that write can itself be missed, have whatever reports `muse.ingest` cross-check each failure entry against the existing...
- [[MUSE010 - Capture-time routing step 12 in scripts muse drops addressed memos into|T010]] — **Next:** open `scripts/muse`, locate step 11 `notify` (~line 797), and write step 12 exactly per the spec above; test with a fixture transcript naming one agent and one naming nobody.
- [[MUSE008 - Stamp addressed lumen when a recording is spoken to Lumen|T008]] — **Next:** At ingest, detect a leading spoken "Lumen" in the transcript (the user already opens such recordings with "I'm sending a message over to the agent" / "Lumen, …") and write `addressed: lumen` into the item's frontmatter....
- [[MUSE002 - 2026-07-21 ingest faults - missed recording, duplicate items, refusal|T002]] — **Next:** Fix three faults observed in the 2026-07-21 batch, all rooted in the same iCloud FileProvider behaviour: an iCloud drop does not raise an FSEvent, so a WatchPaths-only trigger misses it, and...
