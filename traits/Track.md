---
description: Track Trait — the anchor is driven through a planning + backlog lifecycle (the "drive loop"). Composable capability trait; co-requires the Backlog facet. Its on-disk tree is the {slug} Track/ folder (rename to {slug} Track/ deferred).
---

# Track

Capability trait declaring that an anchor is actively driven through a planning + backlog lifecycle (the "drive loop").

## What it is

**Track declares that an anchor is actively driven through a planning + backlog lifecycle — the "drive loop."** Adding the Track trait turns on everything loosely associated with the anchor's planning tree: a backlog with workflow states, feature docs, optional roadmap / PRD / system-design / questions, and the drive-loop skills that operate over them (`/groom`, `/triage`, `/crank`, `/mint`, `/land`, `/fortify`, `/feature`, `/ask`, `/audit q`).

Track is **orthogonal to what the anchor *is***. `Code` says "this is a code project"; `Track` says "this project is run through a backlog and planning system." A code project, a writing project, a skill, or a knowledge hub can all be Tracked — or not. The two compose: `traits: [Code, Track]` is the common case; `traits: [Track]` alone is a non-code project that still has a backlog (e.g. a wiring / ops effort).

> **Naming note (updated 2026-06-01).** Per the *trait-name = folder-name* principle, the Track tree lives at `{slug} Docs/{slug} Track/`. The rename `{slug} Track/` → `{slug} Track/` ships as part of [[F094 — Anchor docs folder restructure — Track _ User _ Architecture _ Dev|F094]] (Q5=A — F094 absorbs the deferred rename). The word `Plan` is freed for a future top-level strategic-plan *document* inside the tree.
>
> **Migration phasing (per F094 Q4):** hybrid Phase 1 migrates SKA + CAB + CAE + the user-named active set; Phase 2 sweeps all remaining anchors. During Phase 1, mixed-state anchors (some at `{slug} Track/`, some at `{slug} Track/`) are expected — references to either folder name resolve to "the Track tree."

## How it's detected

- **Trait:** `Track` appears in the anchor's `.anchor` `traits:` list. One-line lookup; no file inference (per [[CAB Aspects]]).
- Corroborating (not the detection rule): a Track anchor has a `{slug} Track/` folder containing at minimum `{slug} Backlog.md`. Detection is still the declared `traits:` field — the folder is the manifestation, not the test.

## The Track tree

The tree rooted at `{slug} Docs/{slug} Track/` (the dispatch page is [[CAB Track Dispatch]]). One **required core** + optional parts; list only what exists for the anchor.

Per [[F094 — Anchor docs folder restructure — Track _ User _ Architecture _ Dev|F094]] (2026-06-01), Track holds **work tracking** only. Design content (PRD, UX Design, System Design) moves to `{slug} Design/` (see [[CAB Design Dispatch]]). The Track tree stays focused on Backlog + Features + Roadmap.

| Part | File / folder | Facet spec | Required? |
|---|---|---|---|
| Dispatch | `{slug} Track.md` | [[CAB Track Dispatch]] | Yes — the tree's folder-note + index |
| **Backlog** | `{slug} Backlog.md` | [[CAB Backlog]] | **Yes — the irreducible core of the drive loop** |
| Features | `{slug} Features/` | [[CAB Features]] | Optional (present once any feature doc exists) |
| Roadmap | `{slug} Roadmap.md` | [[CAB Roadmap]] | Optional |
| Discussion | `{slug} Discussion.md` | tracking-level discussion (planning trade-offs only) | Optional |
| Icebox | `{slug} Icebox.md` | [[CAB Icebox]] | Optional |
| Inbox | `{slug} Inbox.md` | [[CAB Inbox]] | Optional |
| queries snapshot | `{slug} queries.md` | [[query]] | Optional (regenerated by bare `/ask`; also holds anchor-level questions) |

**Moved out of Track per F094** (now live in `{slug} Design/`): PRD, System Design (folded into [[CAB Architecture|Architecture]]), UX Design. See [[CAB Design Dispatch]].

**Triage is not a tree file.** Per F075 the per-anchor `{slug} Triage.md` is retired — the single triage surface is `~/ob/kmr/Q.md`, into which a Track anchor's section is generated by `/triage`. (Legacy `{slug} Triage.md` files may still exist; they're being phased out.)

The **backlog horizons** (`## Now` / `## Next` / `## Later`) and **workflow states** (`[Ready]` / `[Active]` / `[Questions]` / `[Verify]` / `[Done]` / `[Blocked]` / `[Waiting]` / `[Watching]`) are owned by [[DAS Backlog]] and [[workflow]]; the Backlog facet ([[CAB Backlog]]) carries the numbering policy and row format.

## Wiring an anchor for Track

1. **Declare the trait.** Add `Track` to the `.anchor` `traits:` list, e.g. `traits: [Code, Track]` or `traits: [Track]`.
2. **Create the tree root.** `{slug} Docs/{slug} Track/` with the dispatch page `{slug} Plan.md` per [[CAB Plan Dispatch]] (H1 `# {slug} Plan`, dispatch table, one row per planning doc that exists).
3. **Create the required core.** `{slug} Backlog.md` with the horizon H2s (`## Now` / `## Next` / `## Later`, plus `## Active` / `## Ready` as used) per [[CAB Backlog]] + [[DAS Backlog]]. An empty-but-well-formed backlog is valid.
4. **Add optional parts as needed** — Features/, Roadmap, PRD, System Design, etc. — each per its facet spec. Don't pre-create empties; add a part when its first real content arrives.
5. **Link from the anchor page.** Add a `Plan` row to the anchor page's dispatch table pointing at `{slug} Plan.md` (per [[CAB Anchor Page]]).
6. **The drive loop now applies.** `/groom`, `/triage`, `/crank`, `/mint`, `/land`, `/feature`, `/ask`, `/audit q` all operate on this anchor; its section appears in `~/ob/kmr/Q.md`.

Minimum viable Track anchor = trait declared + `{slug} Track/{slug} Plan.md` + `{slug} Backlog.md`. Everything else is additive.

## Format

Structural expectations on a Track anchor as a whole:

- The tree lives under `{slug} Docs/{slug} Track/` (one level below the standard Docs folder).
- Exactly one Backlog per anchor (the Backlog facet is cardinality-one).
- Source order of backlog rows is preserved; `/triage` and `Q.md` never reorder the backlog file (re-ordering is a Q.md-only behavior).
- Feature docs are `F<NNN> — <Title>.md` (zero-padded three digits) under `{slug} Features/`, monotonic-forever numbering per [[CAB Backlog]].

## Constraints

- **Co-requirement.** The `Track` trait **requires the `Backlog` facet** — declaring `Track` mandates a `{slug} Backlog.md`. (A Track anchor with no backlog is a violation; audits flag it.)
- **Cardinality: many / composable.** `Track` is a capability layered on top of the anchor's identity trait — it is *not* one of the single-valued `{Simple, Topic, Code, Paper}` set.
- **Composition.** Legal with `{Topic, Code, Paper, Skill}`. **Excludes `Simple`** (a Simple anchor is "just a folder + marker, nothing else" — adding a drive loop makes it not Simple). See the composability matrix in [[CAB Aspects]].
- **Exactly one Backlog** (inherited from the Backlog facet's own cardinality constraint).

## Expected Usage

- **Most common pairing: `Code + Track`** — a code project run through a backlog. Also common: `Skill + Track` (e.g. [[SKA]] itself), and `Track` alone for a non-code ops/wiring effort.
- **`Track` is the prerequisite for the drive-loop skills.** If an anchor isn't getting `/crank` / `/groom` attention, it's usually because it lacks `Track` (or its backlog is empty).
- **Typical scale:** 5–50 active backlog rows; more than that usually wants `## Later` / `## Icebox` to absorb the tail.
- **Don't pre-create empty optional parts** — a Roadmap/PRD/Icebox should appear when there's real content, not as scaffolding.
- **Cadence is separate.** *Whether* an anchor is Tracked (this trait) is independent of *how aggressively* it's driven (the Drive vs Lean cadence axis, [[F077]]). Don't conflate `Track` (has a drive loop) with `Drive` (the aggressive cadence mode).

## Skills and audits that attach

- **Drive-loop skills:** [[groom]], [[triage]], [[crank]], [[mint]], [[land]], [[fortify]], [[feature]], [[query]].
- **Audit:** `/audit q` (fix-by-default per F076) validates the backlog + Q.md consistency; `/audit aspects` (proposed, F090 Phase 6) will check the `Track ⇒ Backlog` co-requirement and the composability matrix.
- **Disciplines:** [[workflow]] (state graph + Definition of Ready), [[DAS Backlog]] (Now/Next/Later), [[ask-format]] (question/verify surfaces).

# BRIEF

*(Maintainer note — editing the Track *trait* spec.)*

- **Inclusion/exclusion:** this page holds what the trait means, how it's detected, the tree shape, wiring, constraints, and the attach list. It does **not** hold per-anchor backlog rows or feature lists, drive-loop skill runbooks, the workflow state graph / Definition of Ready (owned by [[workflow]]), or markdown rules (owned by [[R-markdown]]) — link, don't inline.
- **Two-place edit on tree-shape change:** the *Track tree* table cites each part's facet spec; when the tree changes, update the table **and** that facet spec — don't pile facet detail into this page.
- **Composability is audit-enforced:** the `Track ⇒ Backlog` co-requirement and `excludes Simple` are what `/audit aspects` will check (F090 Phase 6); keep them surfaced in *Constraints*. Full matrix lives in [[CAB Aspects]].
- **Active-migration watch:** the `{slug} Plan/` → `{slug} Track/` rename ships under [[F094 — Anchor docs folder restructure — Track _ User _ Architecture _ Dev|F094]]; prefer `{slug} Track/` in examples, flag legacy `{slug} Plan/` only where phasing matters.
