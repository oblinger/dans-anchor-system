---
description: "deferred work — roadmap milestones M0–M5"
---
# Warden Backlog

## Active

_None._

## Ready

## Now

- **F229 — Retire bespoke vault-wide hooks — M4 completion** [Questions] — → [[F229 — Retire bespoke vault-wide hooks — M4 completion|F229]] Q1 (how does `audit-on-write` coverage go vault-wide?) is the one pending question — an adoption decision reserved to the user per the nothing-is-auto-adopted doctrine. With M4a landed, `audit-on-write.sh` is the sole surviving bespoke hook (the audit-q-autofire and compact/markdown-write drivers are already gone from settings.json), live vault-wide and double-firing with Warden's trait-gated doc-fire in adopting anchors; the answer decides the coverage of the swap, then the retirement is mechanical.

## Next

## Later

## Done

- **M4a — audit-on-write fixer parity in the Warden doc-fire** [Done] — **built + LIVE 2026-07-05.** Warden's on-write path now behaviorally subsumes the bespoke F177 hook **by construction**: `warden_docfire.fire_on_write` owns resolve/match from the compiled doc-rule rows, then delegates check + fix execution to `audit-plan.execute_on_write` itself — same fixer registry, same never-delete alnum-subsequence floor, same fix→re-check→message fallthrough. `fix::` now parses into the rule/IR row (`warden_compile`); `warden_hook.audit_on_write` reports `✓ fixed` + `·` issue lines (why carried through); daemon + Rust paths inherit the change. Pinned by `test_warden_docfire.test_fire_on_write_fixer_parity` (fixable fail repaired in place, unfixable messaged with why, steady-state clean); all suites + differentials + corpus green; live smoke through the installed Rust dispatcher steers the R-messages-01 fail. Note: **zero live rules currently carry a markdown `fix::`** (the F004-era annotation was lost in an R-markdown migration), so today's live behavior is message-only on both engines — the fixer parity is capability parity, exercised by the fixture.

- **Design-rules catalog (F218)** [Done] — **Q3 answered (A) + executed 2026-07-05.** All nine mined families upgraded into the live catalog: [[R-arch]] gained 5 children (15 rules — single-source-of-truth, one-path, interfaces-folder, factory-pegboard, ownership), [[R-process]] gained 4 (14 rules — design-gate, stable-ids, exception-discipline, wrapper-cli); umbrellas `include::` their children; [[Rulesets]] catalog rows updated; corpus 465 → **499 rules / 131 rulesets**. SC verified (trait-gated activation, no cross-family leak). **The SC check exposed + fixed a real engine gap**: `include::` was never flattened into the trait index — every umbrella trait keyed zero rules, so "adopt the umbrella" had always been a no-op; `compile_corpus` now flattens transitively (`test_include_flatten`), and `R-warden-dev-02` sharpened to per-rule reachability so undeclared umbrella paths to separately-reachable rules aren't false-flagged. Housekeeping executed (legacy oblinger-rules → redirect at [[R-ob]] with distinct residue absorbed first; HA sync pointer moved; sets/README updated). Adoption stays per-app — first app adoption pending. M4's surface-migration remainder split to M4a (fixer parity, Ready) + M4 Q1 (vault-wide adoption). → [[F218 — Design-rules catalog — ship with skills, adopt per-application|F218]]

- **Deny veto path + R-pathguard (F131 realization)** [Done] — **built + LIVE 2026-07-05.** The `deny` action is now a real veto: a `DENY: `-sentinel steer at a PreToolUse event becomes `permissionDecision: deny` with the redirect as the reason — mirrored in both dispatchers (`warden_hook.emit` / Rust `hook.rs emit`, differential-gated), fail-open at every other moment (deny is `tool:pre`-only per [[Warden Semantics]]). `ctx.event` ({tool, target, input}) joins the interpretation environment, plumbed Python hook → daemon request ← Rust hook. First consumer: `RULESET R-pathguard` (script-owned surfaces — backlog/queries files on Edit+Write, feature-doc Q regions with section-interior matching, Atlas), keyed by the `pathguard` trait, dogfooded on the SKA worktree anchor and live-proven through the installed Rust dispatcher (backlog Edit denied with the `state task` redirect; benign Edit passes). SKA's F131 substrate feature closes on this — the Warden engine IS the check-paths substrate; no second PreToolUse binary. → [[F131 — Hooks — fast inner-loop check substrate (path-rule alerts first)|F131]]

- **Backtick-wrapped `where::` canon (F172 realization)** [Done] — **swept + verified 2026-07-05.** `strip_ticks` in `warden_compile` + `audit-plan` (a single surrounding pair; prose selectors with interior code spans untouched; bare legacy accepted); 60 `where::` lines across 57 ruleset files wrapped; IR + audit-plan match sets byte-identical to pre-swap baselines. Bug fixed en route: `R-cli-01..08` had been backticked *before* any parser stripped ticks — the whole family was silently dead (selector parsed as a junk bare glob); live again post-swap. `R-ruleset-12` + [[FCT Ruleset]] § Where clause document the canon. `FCT Query.md` excluded from the sweep (query surfaces owned by another agent this pass — its 2 bare lines sweep later; the parser tolerates both forms).

- **M5 — Perf hardening** [Done] — **landed 2026-07-05.** All three pieces: *profiling* was already standing (F213 benches + the CI perf gate: 2.98/3.71 ms live path); *budget policy* resolved **advisory-first** (PRD Q3) — both dispatchers now time every moment fire against the PRD budgets (tool:pre 2 ms / post+write 10 ms / else 100 ms) and log `OVER-BUDGET` lines, never dropping a rule (demote-to-audit deferred until advisory evidence shows a persistent offender); *cache invalidation under stress* proven by `test_warden_stress.py` (30-round recompile-cache churn with zero false hits/misses, 10-round daemon warm-Corpus artifact rewrites, F215 reval-store external reset + 40 alternating writers without corruption). Suite wired into CI alongside `test_warden_reval`.

- **Re-evaluation economy — the significant-edit gate (F215)** [Done] — **built + LIVE 2026-07-05** (the remaining agent-executable M8 piece; groomed out of the roadmap park the same day). `warden_reval.py` (per-(rule, file) last-evaluated store with atomic writes + lazy `FileView`/`DiffView` — reads never raise); compiler now **emits** residual-`if::` guards (previously a non-vocabulary `if::` earned a `guard_py` name but no function — the rule could never fire) and marks `file.`-referencing rules `file_bearing`; `warden_fire.fire` binds `ctx.file` per (rule, event-file) and advances the record only after the body actually executes, so sub-threshold edits **accumulate** until they jointly cross the gate; `file_path` plumbed through Python hook, daemon request, and Rust dispatcher (one optional field — selection untouched). All four open Qs resolved agent-side (engine-policy; verdict persists under throttle per Q1). `test_warden_reval.py` pins the Success Criteria fixture (first fire full / typo edit throttled with verdict persistence / section edit re-judges / accumulation); all 10 unit suites + cargo + both differentials + corpus 7/7 green; live corpus recompiled, daemon respawned on the new code, live smoke clean. Semantic-update levels (`file.diff.level`) stay a later milestone. → [[F215 — Re-evaluation economy — the significant-edit gate]]

- **Conversation-content gating (F217)** [Done] — **built + LIVE 2026-07-05** (same-day follow-through on F216). `agent.turn` lazy view (6 content members, TURN_CAP head+tail truncation, shared tail read, R4 error values) + `agent.response` alias; compiler `turn_bearing` mark with R4 wholesale-skip in both fire paths; `ask_oracle` real (blocking, prompt-hash cached, fail-silent, `WARDEN_ORACLE=1` spawn); both loop-prevention walls live (oracle sessions moment-silent in both hook engines; daemon once-per-`(rule, session, turn)` dedup — steer continuations keep the turn key). Fixtures green incl. R-ex-11/12 content rules + the loop fixture; differential + perf gate pass; live corpus recompiled, daemon restarted. Deliberately deferred per resolved Qs: `turn.prior(n)`, async live oracle. → [[F217 — Conversation-content gating — rules on what was said]]

- **Agent-state model (F216)** [Done] — **built + LIVE 2026-07-05.** `engine/warden_agent.py`: the mechanical classifier (full ladder — liveness → in-flight → turn-end → decay; Q = T1 AskUserQuestion dialog + T2 text heuristic; W = open harness tasks) behind a lazy `ctx.agent` view (`state`/`skill`/`is_asking`/`state_seconds`/`open_tasks`; reads never raise). Session registry + bounded moment ledger live in the daemon (`fire_rules` grew an optional `session` field, forwarded by the Rust hook); history rebuilds from the transcript tail, so a sparse ledger degrades recency, not correctness. Scripted state fixtures (`test_warden_agent.py`, incl. the asking-only fixture rule) + extended daemon test + differential + perf gate all green; live daemon restarted onto the new code. → [[F216 — Agent-state model — sensing what the agent is doing]]

- **F209 — Unified trigger taxonomy + `when::` language (M0 freeze)** [Done] — **frozen + verified 2026-07-05.** Every legacy surface maps to a canonical dispatcher-reachable moment; the no-orphans sweep caught one real orphan (`R-query-14` keyed verbatim at `skill:post:audit-q` — unreachable) and fixed it by realizing Q3 in the compiler (`canonical_moment` normalizes authored `skill:post:X` → `skill:pre:X`); the property is a standing CI test. Live corpus recompiled. → [[F209 — Unified trigger taxonomy + when language]]

- **F210 — Conjunction binding + indexing (M0 freeze)** [Done] — **wiring confirmed 2026-07-05.** The frozen `if::` vocabulary compiles to declarative guards evaluated identically in both engines (every op × key differential-tested); moment indexing is the IR dispatch buckets, pinned by unit + cargo tests. M0 language freeze complete (with F209). → [[F210 — Conjunction binding + indexing]]

- **F223 — Markdown-discipline layout rules** [Done] — **built + LIVE 2026-07-05** (roadmap item 8, the deliberate stress test). `RULESET R-progressive` (2 conditional/multi-check rules — never-both self-masthead+`:>>`-breadcrumb; section spacing) in [[DSC progressive-disclosure]], wired into [[R-doc]], firing on every markdown write via audit-on-write. **13 genuine findings / 784 docs, 0 false positives** after pruning two over-aggressive directions that repo-wide validation exposed (anchor-page classification isn't reliable per-file). Verdict-identical on both engines; corpus re-blessed; the F177 hook caught the one design bug live on the first edit. → [[F223 — Markdown-discipline layout rules]]

- **F213 phase 2 — Rust resident daemon** [Done] — **built + LIVE 2026-07-05** (roadmap item 9 — the live push is complete). `warden_daemon.py` (warm resident interpreter: IR + 459 rules preloaded on a Unix socket, per-rule reference-path fire + warm doc-fire, auto-reload + idle-exit/respawn) + `warden-rs hook` (the live dispatcher in Rust — Python bodies cross as IPC round-trips, re-interleaved into exact reference fire order); installed live via `warden install --rust`, verified firing R-warden-dev through the daemon, kill-switch clean. Differential gate 7/7 in CI; benched 2.98/3.71 ms vs Python's ~24 ms (~7×). → [[F213 — Rust performance implementation + ms budget]]

- **F214 — Rule-system testing regime** [Done] — **all five layers live 2026-07-05.** The perf gate (last layer, rode F213 phase 2) joined CI: `test_warden_perf.py` fails on hot-path regression (3.22/3.49 ms local vs 20/30 ms CI-tolerant ceilings); the differential layer gained the live-dispatcher gate (`test_warden_hook_rust.py`, Rust hook ≡ Python hook). Standing CI: 8 unit files + golden corpus (both engines) + two differentials + perf; live-e2e (F221) stays the local/manual gate. → [[F214 — Rule-system testing regime]]

- **Activation self-audit rules (F219)** [Done] — **built + LIVE 2026-07-05** (M1 gate had cleared 2026-07-02; groomed out of Later). `warden compile` stamps `declared_traits` (vault `.anchor` walk) into the IR; `R-warden-dev-02` audits it at `session:start` — moment-rule traits no anchor declares = dead wiring; `warden-selftest` exempt. **Caught a real bug on first run**: `query` (keying R-query-14, the pilot autofire steer) was declared by no anchor — the flagship rule was live-inert; fixed by the Warden anchor adopting `query`. Fixture + live-silent verification green. → [[F219 — Activation self-audit rules — base-trait + ruleset-reachability]]

- **F220 — Live hook install + kill switch** [Done] — **LIVE in the user's environment 2026-07-02.** Dispatcher + kill switch + `warden` CLI shipped; **global-installed** into [settings.json](~/.claude/settings.json) (all 7 existing hooks preserved, backed up, valid JSON), corpus compiled to `~/.warden/`, CLI symlinked to [warden](~/bin/warden). Verified end-to-end against the global config: a real agent fired all 4 pilot moments; `warden off` → zero firings. Inert-by-default, ~20ms/hook, cannot-block (fail-safe). → [[F220 — Live hook install + kill switch]]
  - **Follow-on:** ✅ *first real dogfood live 2026-07-02* — `R-warden-dev` (`warden-dev` trait on `warden/.anchor`) fires a genuinely-useful `session:start` orientation steer live in the Warden anchor, proven through the real dispatcher (fires in-anchor, inert elsewhere, kill-switchable). Warden now audits its own dev workflow. ✅ *doc-fire on write live 2026-07-02* → **F222** below.

- **F221 — Live-integration test class** [Done] — **built + PASSING 2026-07-02.** `live-e2e.py` drives a real `claude -p` agent and proves the hook fired via the log; both cases green (enabled → all 4 moments; `warden off` → none, kill switch works live). Caught the live-only path bug on first run. Local/manual gate (needs `claude`+auth). → [[F221 — Live-integration test class]]
  - **Next:** widen coverage as the pilot surface widens; decide CI-runnable form (in-process `test_warden_hook.py` is the CI layer).

- **F213 — Rust performance implementation (phase 1)** [Done] — *pulled forward from M8 per the live push.* **Selection engine built + differential-green 2026-07-02.** Rust crate at `warden/rs/` (`warden-rs` bin) consumes `rules-ir.json` and computes the fire plan **byte-for-byte identical** to the Python reference — indexed dispatch + active-set gating + declarative `eval_guard` (all ops) + `tell`/`deny` steers; Python-body/guard rules recorded as owed resident-interpreter round-trips (not executed in Rust). Standing differential gate (`test_warden_rust.py`, live corpus × 6 trait sets + synthetic fixtures, 7/7) + 5 `cargo test` units + CI `rust-engine` job. Cold bench **2.65 ms** (incl. full IR parse), ~7.5× the Python cold path — clears the ms budget at the cold bound. → [[F213 — Rust performance implementation + ms budget]]
  - **Next (phase 2):** the warm **resident-Python interpreter + IPC** for `body_py`/`guard_py` execution (Rust owns selection; Python bodies cross the boundary preloaded, not per-call startup). Then wire the Rust selection path into the F220 dispatcher.

- **Moment-corpus golden case (F214 golden layer)** [Done] — **built 2026-07-02.** The golden corpus now spans the **moment fire path**: a `warden-moment` engine adapter fires a live moment against a fixture anchor and locks the emitted steers (`moment-001-selftest-write` blesses the `write:markdown` selftest steer). En route, two latent corpus-runner defects fixed: `--engine <name>` now genuinely overrides the per-case engine (the "warden differential" CI job was silently running audit-plan, not warden), and each case pins to its own engine's signature (doc-rule hash vs. moment dispatch+body hash). Stale doc baseline re-blessed (`1069…`→`3869…`, verdicts unchanged). Full suite + both corpus engines green. → [[F214 — Rule-system testing regime]] (golden layer).

- **F222 — Doc-fire on write** [Done] — **built + LIVE 2026-07-02** (roadmap item 7). The doc-fire (audit-on-write) path fires live in the `write:markdown` moment: an anchor that adopts the `audit-on-write` trait has its doc-audit rules run on each markdown write, steering `fail` verdicts (never `error` — unimplemented checkers are rule-infra, not the writer's problem). Dispatcher wiring + opt-in trait gate + `test_warden_hook.test_audit_on_write`; live-proven on the Warden anchor (adopted first; 0/38 real docs noisy under fail-only). → [[F222 — Doc-fire on write]]

- **M1 — Rule compiler / installer** [Done] — design + skeleton: active-set resolution (per anchor), index selection, per-moment pre-compilation, the install + fire contract. Pilot by porting `R-query-14` to fire via the compiler. **Engine complete + live-installed (F220) 2026-07-02.** → [[F211 — Rule compiler and installer]]
  - **Next:** **Engine complete 2026-07-02 — every Success Criterion met at the engine level.** `warden_compile.py` (single + `--root` corpus compile of all 449 vault rules; when-rules→`moments`, tier doc-rules→`doc_rules` with `check`/`judge`/`track` actions; per-function constant encapsulation; recompile cache on the scan-index hash), `warden_fire.py` (moment dispatcher — indexed dispatch + active-set gating), and `warden_engine.py` (the lazy scan→compile→fire reference loop = F212's oracle). `R-query-14` fires end-to-end; 4 engine test suites green (`test_warden_{scan,compile,fire,engine}.py`). **Only remaining piece = live-hook install (M4):** wiring the compiled moment-modules into the real Claude Code hook surface — high-blast-radius, its own session. Vault-wide active-set adoption also awaits the trait→ruleset bindings per **F218** (propose→review), but the engine resolves active-set from whatever `.anchor` traits are declared.

- **M2 — Python reference implementation** [Done] — **reference loop + both fire paths built 2026-07-02.** `warden_engine.py` (lazy scan→compile→fire, the behavioral oracle; `R-query-14` fires through it) **plus `warden_docfire.py`** — the IR-driven doc-audit fire path, **verdict-identical to `audit-plan --run`**: the golden corpus runs through a `warden` engine adapter and matches the audit-plan-blessed `expected.json` verdict-for-verdict (4/4, same signature); `test_warden_docfire.py` is a standing differential test (warden ≡ audit-plan every case). `WardenEngine.fire_audit` owns the audit surface; the **"golden-corpus suite passes" Success Criterion is met.** Remaining: the moment-side steer **snapshot API** + mapping the in-process dispatcher to real hook events for e2e. Reuses `audit-plan.py`. → [[F212 — Python reference implementation]]
- **M3 — Rust performance implementation** [Done] — fire-time hot path under the per-moment ms budget; behavior-identical to the Python reference (differential-tested). Landed via F213 phases 1+2. → [[F213 — Rust performance implementation + ms budget]]
