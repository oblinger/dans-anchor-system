---
description: Warden queries — mechanically rendered from the backlog by triage (Verifications / Ready+Next / Questions). Do not hand-edit; edit the backlog rows.
---

# [A]  [[Warden|Warden]]  -  Ready 5    Questions 0   |   Now 5    Next 2    Later 4    Verify 0    Icebox 0

## Ready
- [[F216 — Agent-state model — sensing what the agent is doing]] — **Next:** implement the agent-state model — the `asking` / `paused` states (AskUserQuestion moment + text heuristic) and the `agent.state_seconds` / `agent.open_tasks` properties; design is user-approved and M2 has shipped....
- [[F209 — Unified trigger taxonomy + when language]] — **Next:** map each legacy trigger surface (`compact`, `markdown-write`, `skill:*`) onto a canonical moment path, verify no orphans against `warden_hook.event_to_moments`, then freeze the M0 `when::` surface....
- [[F210 — Conjunction binding + indexing]] — **Next:** confirm conjunction binding (`if::` guards → `eval_guard`) and moment indexing are fully wired in the compiler + fire path, then freeze the M0 `where::`/`if::` surface. No user input needed.
- Markdown-discipline — **Next:** author the `R-markdown` ruleset (rules for dispatch-table presence-by-context + `:>>`-breadcrumb + blank-line placement, each a multi-check body) with `where::`/`check::` selectors; implement their...
- [[F213 — Rust performance implementation + ms budget]] — **Next:** the warm **resident-Python interpreter + IPC** for `body_py`/`guard_py` execution (Rust owns selection; Python bodies cross the boundary preloaded, not per-call startup)....
